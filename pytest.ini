[pytest]
minversion = 7.0
addopts = --maxfail=1 -q --cov=clipboard_manager --cov-report=term-missing
testpaths = tests
python_files = test_*.py
markers =
    gui: mark tests as GUI / integration tests that require a display (skipped in CI)
